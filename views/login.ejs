<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Retrotech</title>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #00d2ff;
      --secondary: #3a7bd5;
      --accent: #ff00c1;
      --bg-dark: #05070a;
    }

    * { box-sizing: border-box; }

    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: 'Plus Jakarta Sans', sans-serif;
      background-color: var(--bg-dark);
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    /* --- Animated Starry Background --- */
    .bg-animation {
      position: fixed;
      top: 0; left: 0; width: 100%; height: 100%;
      z-index: -1;
      background: radial-gradient(ellipse at bottom, #1B2735 0%, #090A0F 100%);
    }

    /* Create animated stars using box-shadow shadows */
    .stars {
      width: 2px;
      height: 2px;
      background: transparent;
      box-shadow: 443px 1827px #FFF, 342px 143px #FFF, 1283px 1822px #FFF, 1542px 421px #FFF, 100px 100px #FFF, 800px 600px #FFF, 1200px 200px #FFF, 400px 900px #FFF;
      animation: animStar 50s linear infinite;
    }
    .stars:after {
      content: " ";
      position: absolute;
      top: 2000px;
      width: 2px;
      height: 2px;
      background: transparent;
      box-shadow: 443px 1827px #FFF, 342px 143px #FFF, 1283px 1822px #FFF, 1542px 421px #FFF, 100px 100px #FFF;
    }

    @keyframes animStar {
      from { transform: translateY(0px); }
      to { transform: translateY(-2000px); }
    }

    /* --- Floating Card Animation --- */
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-15px); }
      100% { transform: translateY(0px); }
    }

    .login-container {
      width: 100%;
      max-width: 420px;
      padding: 20px;
      animation: float 6s ease-in-out infinite;
    }

    .login-box {
      background: rgba(15, 20, 30, 0.7);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 30px;
      padding: 50px 40px;
      text-align: center;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
    }

    /* --- Logo & Text --- */
    .logo-container img {
      height: 70px;
      margin-bottom: 20px;
      filter: drop-shadow(0 0 15px rgba(0, 210, 255, 0.5));
    }

    h2 {
      color: #fff;
      font-weight: 700;
      font-size: 2rem;
      margin-bottom: 5px;
      letter-spacing: -1px;
    }

    p.subtitle {
      color: #94a3b8;
      font-size: 0.9rem;
      margin-bottom: 30px;
    }

    /* --- Inputs --- */
    .input-group {
      margin-bottom: 20px;
    }

    .input-group input {
      width: 100%;
      padding: 16px 20px;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      color: #fff;
      font-size: 1rem;
      transition: 0.3s;
      outline: none;
    }

    .input-group input:focus {
      border-color: var(--primary);
      box-shadow: 0 0 20px rgba(0, 210, 255, 0.2);
    }

    /* --- Submit Button --- */
    button {
      width: 100%;
      padding: 16px;
      border-radius: 15px;
      border: none;
      background: linear-gradient(90deg, #00d2ff, #3a7bd5, #00d2ff);
      background-size: 200% auto;
      color: white;
      font-weight: 700;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.5s;
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 10px 20px rgba(0, 210, 255, 0.3);
    }

    button:hover {
      background-position: right center;
      transform: scale(1.02);
      box-shadow: 0 15px 30px rgba(0, 210, 255, 0.5);
    }

    /* --- Footer Link --- */
    .footer-text {
      margin-top: 25px;
      color: #64748b;
      font-size: 0.85rem;
    }

    .footer-text a {
      color: var(--primary);
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s;
    }

    .footer-text a:hover {
      color: var(--accent);
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="bg-animation">
    <div class="stars"></div>
  </div>

  <div class="login-container">
    <div class="login-box">
      <div class="logo-container">
        <img src="https://i.ibb.co/HTjXVFtf/recto-logo-1.jpg" alt="Logo">
      </div>
      
      <h2>Retrotech</h2>
      <p class="subtitle">Secure Employee Portal</p>

      <% if (locals.error) { %>
        <p style="color: #ff4d4d; font-size: 0.85rem;"><%= error %></p>
      <% } %>

      <form action="/login" method="POST">
        <div class="input-group">
          <input type="text" name="username" placeholder="Username" required>
        </div>

        <div class="input-group">
          <input type="password" name="password" placeholder="Password" required>
        </div>

        <button type="submit">Log In</button>
      </form>

      <div class="footer-text">
        Forgot password? <a href="mailto:hiring@retrotech.in?subject=Login%20Support%20Request">Contact Admin</a>
      </div>
    </div>
  </div>

</body>
</html>
